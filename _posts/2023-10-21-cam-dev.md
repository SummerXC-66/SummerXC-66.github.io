---
layout: post
title: Cam 开发：音频读取与设备联网备忘
date: 2023-10-21
category: 编程
tags: [音频, librosa, Socket, 嵌入式]
---

整理自学习空间「Cam开发」。原稿混了两块：Python 读音频，以及设备连 Wi-Fi / 服务器的常量。Wi-Fi 密码和服务器 IP 已去掉。

## 一、librosa 读音频

参考：[librosa IO formats](https://librosa.org/doc/latest/ioformats.html#read-file-like-objects)

```python
import librosa
import soundfile as sf

filename = librosa.ex("trumpet")
data, samplerate = sf.read(filename, dtype="float32")
data = data.T
data_22k = librosa.resample(data, orig_sr=samplerate, target_sr=22050)
```

大文件可按块读（Blockwise Reading），避免一次载入整段波形。

## 二、设备侧联网常量（嵌入式）

设备连热点、再连采集服务器时，大致是这类字段。发布稿只用占位符：

```c
const char *ssid = "YOUR_WIFI_SSID";
const char *password = "YOUR_WIFI_PASSWORD";
const char *host = "YOUR_SERVER_IP";  // 服务器 IP
const int port = 8888;
```

端口 8888 若临时不用，可在服务器上 `sudo ufw deny 8888`。
